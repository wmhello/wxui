<view class="haibao">
	<swiper  indicator-dots="{{indicatorDots}}" autoplay="{{autoplay}}" current="0" interval="{{interval}}" duration="{{duration}}">
		<block wx:for="{{imgUrls}}">
			<swiper-item>
				<image src="{{item}}" class="silde-image" style="width:100%"></image>
			</swiper-item>
		</block>
	</swiper>
</view>
